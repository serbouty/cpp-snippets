#pragma once /* Include the header file only once when compiling. */

/**
 * Make an arithmetic operation with user input.
 */
class Calculator
{
public: /* Simple declaration, implementation available in Calculator C++ file. */
	double Calculate(double x, char operation, double y) const;
};